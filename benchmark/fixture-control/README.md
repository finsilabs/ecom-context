Control task (design §7.2 task 4): a plain product-launch email within every rule of the large store — 15% is under the 20% cap, email is the active channel, no medical claim is asked for, no stopped channel is touched. It measures over-caution: the tool must not add errors, refusals, `blocked`/`review` mentions, or invented constraints here. Run against the large store: `npx tsx src/benchmark/run.ts --fixture benchmark/fixture-control --store benchmark/fixture-large/store --control`.
