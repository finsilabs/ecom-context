import { describe, it } from 'node:test'; import assert from 'node:assert/strict'; import { evaluate } from './check.js';
const ctx:any={brand:'Dog treats',targets:[{id:'meta',name:'Meta',kind:'channel',status:'paused',aliases:['facebook']},{id:'vip',name:'VIP',kind:'offer',status:'active',aliases:[]}],governance:[{id:'cap',effect:'forbid',domain:'offers',action:'discount',object:'cap',op:'gt',value:20,created_at:'2026-01-01',created_by:'op'},{id:'medical',effect:'forbid',domain:'copy',action:'claim',object:'medical',patterns:['treats? joint stiffness'],created_at:'2026-01-01',created_by:'op'},{id:'compare',effect:'forbid',domain:'offers',action:'compare_at',object:'compare',created_at:'2026-01-01',created_by:'op'}],history:[{id:'stop',decided_at:'2026-01-01',actor:'op',action:'stop',target:'meta',outcome:'negative',status:'confirmed',recorded_by:'operator'},{id:'test',decided_at:'2025-01-01',actor:'op',action:'test',target:'vip',outcome:'negative',params:{discount_pct:30},status:'confirmed',recorded_by:'operator'}]};
describe('context.check evaluator',()=>{it('returns only constraints when proposal is absent',()=>{const out:any=evaluate(ctx,{});assert.deepEqual(Object.keys(out).sort(),['brand','standing_constraints','unresolved_targets']);});it('blocks typed and semantic violations',()=>{const out:any=evaluate(ctx,{targets:['meta'],proposal:{action:'start',target:'meta',discount_pct:35,compare_at:true,text:'treats joint stiffness'}});assert.equal(out.verdict,'blocked');assert.equal(out.violations.length,2);assert.equal(out.pattern_hits.length,1);assert.equal(out.self_check.length,1);});it('detects failed-test repeats and deduplicates conflicts',()=>{const out:any=evaluate(ctx,{targets:['vip'],proposal:{action:'test',target:'vip',discount_pct:35}});assert.equal(out.conflicts.length,1);assert.equal(out.conflicts[0].target,'vip');assert.equal(out.conflicts[0].constraint,'avoid_repeat');});it('resolves proposal audience and never hides blocked precedence',()=>{const out:any=evaluate(ctx,{targets:['missing'],proposal:{action:'keep',target:'missing',compare_at:true}});assert.equal(out.unresolved_targets[0],'missing');assert.equal(out.verdict,'blocked');});});
