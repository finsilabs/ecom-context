{
  "name": "ecom-context",
  "version": "0.1.0",
  "main": "dist/index.js",
  "files": [
    "dist",
    "store",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "tsx --test src/**/*.test.ts src/*.test.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "benchmark": "npm run build && tsx src/benchmark/run.ts",
    "benchmark:large": "npm run build && tsx src/benchmark/run.ts --large",
    "prepare": "tsc",
    "benchmark:pool": "tsx src/benchmark/run.ts --pool",
    "benchmark:regrade": "tsx src/benchmark/run.ts --regrade"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "Local MCP server that holds an e-com operator's operating context so their agent stops rediscovering the business.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "ecom-context": "./dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "@types/node": "^26.4.1",
    "gpt-tokenizer": "^4.0.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
